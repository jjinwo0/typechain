{
    "include":["src"],
    "compilerOptions": {
        //JS File이 생성될 디렉터리 지정
        //build 폴더에 만들어질 코드를 넣을 것이라 지정
        "outDir": "build",

        //어떤 버전의 JS로 TS를 컴파일할지 정함
        "target": "ES6"
    }
}